/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable static export for Vercel deployment
  // Remove if you need API routes
  // output: 'export',
}

module.exports = nextConfig

